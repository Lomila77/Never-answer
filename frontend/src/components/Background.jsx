const Background = ({ componentChildren }) => {
    return <div className="flex w-full flex-col items-center justify-center bg-[#f5fffb]  h-full">
            {componentChildren}
        </div>
}

export default Background;
